import type { UserContextCommand } from "#handlers/commands/index.js";
import type { Newable } from "#utils/types.js";

/**
 * Represents a {@link UserContextCommand | `UserContextCommand`} instance.
 * @public
 */
export type UserContextCommandInstance = Newable<UserContextCommand>;
