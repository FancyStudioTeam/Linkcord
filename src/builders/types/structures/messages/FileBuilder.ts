import { FileBuilder } from "#builders/structures/index.js";

/**
 * Represents a URL that can be set to an {@link FileBuilder | `FileBuilder`} instance.
 * @group Builders/Structures
 */
export type AllowedFileURL = URL | string;
